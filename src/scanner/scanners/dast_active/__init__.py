"""The DAST active tier (``dast-active``): intrusive, detection-only checks run
behind the fail-closed triple gate (contract §11). Importing the scanner module
registers the scanner with the default registry.
"""
